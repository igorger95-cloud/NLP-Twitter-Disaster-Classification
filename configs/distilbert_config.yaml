model:
  name: distilbert-base-uncased
  num_labels: 2

tokenization:
  max_length: 128

training:
  learning_rate: 0.00002
  batch_size: 16
  epochs: 3
  weight_decay: 0.01
  fp16: true

split:
  test_size: 0.2
  random_state: 42
